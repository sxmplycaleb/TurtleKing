# Turtle King Sound Assets

All audio in this directory is **original work generated by this repository**
via `tool/generate_sounds.dart` — synthesized from basic waveforms (sine and
sawtooth tones, noise bursts, decay envelopes) and additive tone/bell
synthesis. No third-party audio, samples, speech, or recordings are used, so
there are no external licensing or attribution requirements. (The two YAMADA
voice clips from M17.3/M17.4 were removed in M17.5 and replaced with a single
game sting SFX — see below.)

**License:** project-owned (same license as the rest of the Turtle King code).
Feel free to regenerate or modify any sound by editing the generator and
re-running `dart run tool/generate_sounds.dart` (seeded — output is
byte-reproducible).

## Asset table

| File | Purpose | Source | License | Attribution |
| --- | --- | --- | --- | --- |
| `card_reveal.wav` | Short crisp card/paper snap when a player reveals their one card (M17.2 refined; M17.3 polish fixed the crackle envelope — it previously blew up to a clamped full-scale burst — and trimmed the peak below full scale) | Generated in-repo (`tool/generate_sounds.dart`) | Project-owned | None |
| `handoff.wav` | Very short soft tactile tap when the phone is passed to the next player — quieter than major events (M17.2 refined) | Generated in-repo | Project-owned | None |
| `hold_out.wav` | Short positive two-note confirmation when a player holds out — **preserved unchanged (M17.2 approved asset)** | Generated in-repo | Project-owned | None |
| `yamada.wav` | Anime/mobile-game "special event" sting (~0.55 s) played when a player calls YAMADA — strong bright attack, quick upward C6→G6→C7 leap, and a playful tremolo-shimmering chime over a soft C-major body (**M17.5**; no spoken word, no human voice, no character imitation) | Generated in-repo | Project-owned | None |
| `reveal.wav` | Bright, satisfying, short anime/game reveal — a quick upward bell sparkle (G6→A6→C7) landing on a clean high "ding" with a soft magical body (~0.36 s) when everyone holds out and hands are revealed (M17.2 refined; M17.3 polish replaced the suspense riser; **M17.4 replaced the sound again** with the current bell-sparkle + ding, faded to silence) | Generated in-repo | Project-owned | None |
| `elimination.wav` | Unmistakable low pitch-bending impact with a decisive second thump when a player is eliminated (M17.2 refined; M17.3 polish trimmed the peak below full scale) | Generated in-repo | Project-owned | None |
| `victory.wav` | Short celebratory C-major arpeggio resolving into a sustained chord for the Turtle King victory (M17.2 refined; M17.3 polish added a fade-out so the resolution ends in silence, never a hard cut) | Generated in-repo | Project-owned | None |

## Format

16-bit PCM, mono, 22050 Hz — universally supported by the `audioplayers`
playback backend (media3/ExoPlayer on Android, AVAudioPlayer on iOS). Total
on-disk size: ~106 KB for all seven files. All sounds are distinct in content
and duration (unique hashes/sample counts, tested); the set is
byte-reproducible from the seeded generator.

## M17.3 final polish — audio audit

Every bundled sound was measured (duration, RMS/loudness, peak/clipping,
leading/trailing silence, sample rate, size) and checked against its intended
character. Results: no sound has any leading silence (no measured latency
contribution), all peaks are now below full scale (no clipping), every tail
fades to silence, and all eight durations remain mutually distinct. The
`hold_out.wav` approved asset is byte-for-byte identical (SHA-256 pinned in
`test/audio_assets_test.dart`); `handoff.wav` was left as-is. See the README
M17.3 section for the finding-by-finding table.

## M17.4 — intelligibility & reveal redesign

The two YAMADA voice clips were redesigned around explicit phoneme segments
(`YAH` → `mah` → `DAH!!!`, with a stop-consonant closure gap before the
`/d/`) so they are clearly intelligible as "Yah-mah-DAH!!!" rather than
three blurred syllables; the deep voice's formants and sub-octave were
rebalanced so it no longer sinks into the low end, and the anime voice's
breath was reduced so it stays bright without being harsh. `reveal.wav` was
replaced with a quick G6→A6→C7 bell sparkle landing on a bright "ding"
(0.36 s). All changes came from the seeded, byte-reproducible generator and
remain project-owned; `hold_out.wav` is byte-for-byte unchanged (SHA-256
`5db1bd…c7829bad`). See the README M17.4 section for the phoneme table and
full audit.

## M17.5 — YAMADA sting replaces the voice clips

The two spoken YAMADA voice clips (`yamada_deep.wav`, `yamada_anime.wav`)
and the **YAMADA Voice** setting (enum, persistence, selector, preview
buttons) were **removed entirely** in M17.5. The YAMADA event now resolves to
exactly one asset, `yamada.wav` — an original, project-owned game sting with
no spoken word, no human voice, and no character imitation. `FeedbackEvent.yamada`
→ `assets/sounds/yamada.wav`, gated by the Sound Effects toggle; haptics stay
independent. The audio engine receives only the resolved asset path — never
player identity, card identity, rank, suit, or `GameState`.

## Licensing note

All assets are original works generated in-repo by `tool/generate_sounds.dart`
and carry the same project-owned license as the rest of the Turtle King code
— free to redistribute commercially under the project's license. No
third-party audio, samples, speech, or TTS engines are used; there are no
attribution requirements.
